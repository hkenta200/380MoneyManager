import React from "react";
import {View, Button, Text} from 'react-native';

const SavingPage = () => {
    return (
        <View style = {{flex : 1, justifyContent: "center", alignContent: "center"}}>
            <Text>Saving View Visual+List</Text> 
        </View>
    );
};

export default SavingPage;